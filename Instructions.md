python -m copilot                 # pull new postings
python -m copilot check           # ask each ATS what's still live
python -m copilot report          # ranked, deduped, GONE-marked
python -m copilot draft <id>      # letter + apply URL + red-flag checklist
python -m copilot apply <id>      # opens the form, pre-filled, stops
python -m copilot mark <id> applied
